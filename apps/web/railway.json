{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"watchPatterns": ["apps/web/**", "packages/shared/**"],
		"buildCommand": "cd apps/web && bunx convex deploy --cmd-url-env-var-name PUBLIC_CONVEX_URL --cmd \"cd ../.. && bun build:web\" && cd ../.. && rm -rf node_modules && bun install --production --ignore-scripts"
	},
	"deploy": {
		"startCommand": "cd apps/web && PORT=8080 bun ./build",
		"healthcheckPath": "/health",
		"healthcheckTimeout": 300,
		"restartPolicyType": "ON_FAILURE",
		"restartPolicyMaxRetries": 5
	}
}
